//Contains global variables used for checksheet manager
//Authentication. Checks if user is logged in on every route change
smApp.run(['$rootScope',
    function($rootScope) {
 $rootScope.cmTitle = "Kutztown University";
    	    }
]);
